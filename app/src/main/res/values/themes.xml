<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AturDana" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
         <item name="colorPrimaryVariant">@color/my_light_primary</item>

        <item name="floatingActionButtonStyle">@style/FloatingActionButton</item>
    </style>

    <style name="Theme.AturDana" parent="Base.Theme.AturDana" />
    <style name="FloatingActionButton" parent="Widget.Design.FloatingActionButton">
        <item name="shapeAppearance">@style/rounded_fab</item>
    </style>
    <style name="rounded_fab" parent="ShapeAppearance.MaterialComponents.SmallComponent">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">50%</item>
    </style>
</resources>